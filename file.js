function foo() {
  if (something && somethingElse || bar) {
    if (innerThing || secondThing && thirdThing) {
      return whatIt || foobar;
    }
  }
}

function bar() {
  if (something && somethingElse || bar) {
    if (innerThing || secondThing && thirdThing) {
      return whatIt || foobar;
    }
  }
}

function baz() {
  if (something && somethingElse || bar) {
    if (innerThing || secondThing && thirdThing) {
      return whatIt || foobar;
    }
  }
}
